<!-- 引用组件的页面模版 -->
<view class='node-topic wrap'>
  <my-component
  wx:for="{{datas}}"
  wx:key="{{item.id}}"
  data="{{item}}"

  ></my-component>

</view>

<loading hidden="{{hidden}}">加载中...</loading>