<!-- 引用组件的页面模版 -->
<view class='new wrap'>
  <my-component
  wx:for="{{newData}}"
  wx:key="{{item.id}}"
  title="{{item.title}}"
  tag="{{item.tag}}"
  icon="{{item.icon}}"
  name="{{item.name}}"
  time="{{item.time}}"
  mes="{{item.mes}}"
  ></my-component>

</view>